example file for first git push
git clone git@github.com:ruokaythere/Gitbasics.git to copy repo to local machine.
git config --global user.name "ruokay" to change the user name to ruokay instead of first last.

git checkout -b <branch_name> two in one command to create a new branch and switch to that branch 
